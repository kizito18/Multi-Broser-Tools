package com.my.Zitos.MultiBroser.Tools.webAutoRefreshOnly.moreFunctions

import com.my.Zitos.MultiBroser.Tools.WebControllerType
import com.my.Zitos.MultiBroser.Tools.webAutoRefreshOnly.viewModelAndWebViewHolder.BrowserAutoRefreshViewModel

fun onBackButtonClickedForWebRefresh(
    webNum: Int,
    browserAutoRefreshViewModel: BrowserAutoRefreshViewModel
) {


    if (webNum ==1) {
        browserAutoRefreshViewModel.webViewHolder1.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 2) {
        browserAutoRefreshViewModel.webViewHolder2.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 3) {
        browserAutoRefreshViewModel.webViewHolder3.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 4) {
        browserAutoRefreshViewModel.webViewHolder4.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 5) {
        browserAutoRefreshViewModel.webViewHolder5.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 6) {
        browserAutoRefreshViewModel.webViewHolder6.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 7) {
        browserAutoRefreshViewModel.webViewHolder7.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 8) {
        browserAutoRefreshViewModel.webViewHolder8.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 9) {
        browserAutoRefreshViewModel.webViewHolder9.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 10) {
        browserAutoRefreshViewModel.webViewHolder10.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 11) {
        browserAutoRefreshViewModel.webViewHolder11.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 12) {
        browserAutoRefreshViewModel.webViewHolder12.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 13) {
        browserAutoRefreshViewModel.webViewHolder13.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 14) {
        browserAutoRefreshViewModel.webViewHolder14.value.webController= WebControllerType.GO_BACK
    }

    if (webNum == 15) {
        browserAutoRefreshViewModel.webViewHolder15.value.webController= WebControllerType.GO_BACK
    }



}