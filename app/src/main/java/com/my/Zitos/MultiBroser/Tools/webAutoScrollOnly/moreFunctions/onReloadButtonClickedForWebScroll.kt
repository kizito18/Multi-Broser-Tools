package com.my.Zitos.MultiBroser.Tools.webAutoScrollOnly.moreFunctions

import com.my.Zitos.MultiBroser.Tools.WebControllerType
import com.my.Zitos.MultiBroser.Tools.webAutoScrollOnly.viewModelAndWebViewHolder.BrowserAutoScrollViewModel

fun onReloadButtonClickedForWebScroll (

    webNum: Int,
    browserAutoScrollViewModel: BrowserAutoScrollViewModel
) {

    if (webNum ==1) {
        browserAutoScrollViewModel.webViewHolder1.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 2) {
        browserAutoScrollViewModel.webViewHolder2.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 3) {
        browserAutoScrollViewModel.webViewHolder3.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 4) {
        browserAutoScrollViewModel.webViewHolder4.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 5) {
        browserAutoScrollViewModel.webViewHolder5.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 6) {
        browserAutoScrollViewModel.webViewHolder6.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 7) {
        browserAutoScrollViewModel.webViewHolder7.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 8) {
        browserAutoScrollViewModel.webViewHolder8.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 9) {
        browserAutoScrollViewModel.webViewHolder9.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 10) {
        browserAutoScrollViewModel.webViewHolder10.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 11) {
        browserAutoScrollViewModel.webViewHolder11.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 12) {
        browserAutoScrollViewModel.webViewHolder12.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 13) {
        browserAutoScrollViewModel.webViewHolder13.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 14) {
        browserAutoScrollViewModel.webViewHolder14.value.webController= WebControllerType.RELOAD_URL
    }

    if (webNum == 15) {
        browserAutoScrollViewModel.webViewHolder15.value.webController= WebControllerType.RELOAD_URL
    }

}