<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">


    <style name="Theme.MultiBrowserToolsPro" parent="android:Theme.Material.Light.NoActionBar">


        <item name="android:enforceNavigationBarContrast" tools:targetApi="q">false</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="q">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>

        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>

    </style>


    <!-- Style for API 26 (Oreo) and above -->
    <style name="Theme.ColorPickerPro" parent="Theme.MultiBrowserToolsPro">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- For lower versions, Theme.ColorPickerPro will inherit properties from Theme.ColorPickerProBase -->
    <!-- You can define additional styles for lower versions here if needed -->


</resources>